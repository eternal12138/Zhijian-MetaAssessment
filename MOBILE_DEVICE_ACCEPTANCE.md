# 移动端真实设备验收清单

本清单用于正式采集前验证手机端录音、恢复和上传链路。每一种计划用于正式测评的“设备 + 操作系统 + 浏览器”组合都应至少完整执行一次，不以桌面浏览器模拟结果代替。

## 建议覆盖范围

- iPhone：当前主流 iOS，Safari。
- Android：当前主流系统，最新版 Chrome 或 Edge。
- 如参与者可能从微信进入，验证是否明确引导到系统浏览器；不把微信内置浏览器作为默认采集环境。
- Wi-Fi 和移动数据各完成一次关键上传场景。

## 验收记录

每次验收记录：测试日期、手机型号、系统版本、浏览器及版本、测试账号、测评运行 ID、执行人、结果、异常截图或日志时间。

## A. 麦克风授权

1. 首次打开测评，选择允许麦克风，确认页面显示“正在录音”且计时增长。
2. 先拒绝权限，确认页面给出可理解的恢复方法，且未录音时不能提交正式任务。
3. 从系统设置重新允许权限，返回页面后应能重新检查并开始录音。
4. 使用蓝牙耳机和手机内置麦克风各录制一次，导出后试听人声、音量和声道。

## B. 系统中断

分别在录音 30 秒后执行以下操作：锁屏 20 秒、切换到其他应用 20 秒、接听或模拟来电、切换横竖屏。返回测评后检查：

- 页面仍能明确显示当前录音状态，不能把已停止误报为正在录音。
- 已产生分片可以继续上传或重试，不静默丢失。
- 任务不能在缺少有效录音时直接提交。
- 完成后导出的音频可连续播放；中断时间点在日志和音频中可解释。

如果某浏览器在锁屏或后台必然停止录音，应将其记录为产品限制，并在正式参与说明中要求保持屏幕常亮、停留页面。

## C. 网络与恢复

1. 录音中关闭网络 30 秒后恢复，确认待上传数量下降至 0，失败分片可重试。
2. 在 Wi-Fi 与移动数据之间切换，确认不会重复提交同一分片。
3. 页面意外刷新后，确认可以恢复未完成测评，且已提交任务不会重复创建。
4. 连续录音至少 20 分钟，检查手机发热、内存、页面响应和最终提交时间。

## D. 交互与布局

1. 在 360–430 px 宽度检查知情同意、设备检查、练习、两项正式任务、问卷和完成页。
2. 打开计算工具，验证草稿纸可用、悬浮计算器可拖动且不完全遮挡题目。
3. 打开软键盘时，输入框、主要按钮和录音状态仍可访问。
4. 放大系统字体一级，检查标题、提示和按钮无截断或重叠。

## E. 数据闭环

1. 管理端可以按账号和完成时间定位该次测评。
2. 下载并试听原始/合并音频，确认包含清晰人声且时长合理。
3. 检查权威转录、问卷、交互事件和完成时间均归属同一测评运行。
4. 执行一次退出/删除演练，确认录音、转录、问卷、候选和编码记录均被纳入关联检查。

## 放行标准

- 关键场景无数据静默丢失、无错误“正在录音”状态、无绕过强制录音提交。
- 音频可播放、有人声，分片数量与服务端记录一致，ASR 能进入识别或明确失败状态。
- P0/P1 异常必须修复后重新全量验证；仅有文字或间距等不影响采集的问题可记录为 P2 后续项。
