# P0 真实场景验收报告

验收日期：2026-08-08

验收环境：Windows 本地开发环境，前端 `127.0.0.1:5173`，后端 `127.0.0.1:8000`

## 结论

当前版本通过角色权限、研究工作流、移动端布局、恢复能力和数据导出验收；在正式采集前仍需完成一次真实麦克风录音闭环，并修复当前环境的 LLM 与 ASR 公网配置。因此结论为：**有条件通过，暂不建议直接开始正式采集**。

## 已通过场景

- 学生、教师、管理员登录及初始密码暂缓流程。
- 教师访问管理员页面时自动返回教师工作台。
- 教师仅看到其负责班级的数据。
- 学生测评任务、题目图片、朗读状态、强制录音门槛、计算器和草稿纸加载。
- 录音未启动时禁止提交，并显示明确重试入口。
- 测评页面刷新后恢复当前测评与任务。
- 转录校订任务切换及失败任务重新排队，无 HTTP 500。
- 双人编码页面按固定批次显示待办。
- 学生仅能看到已经审核发布的报告。
- 管理员用户列表、姓名/账号查找和未分班提示。
- 问卷答题矩阵 CSV 导出，共导出 2 条现有记录。
- 录音与转录 ZIP 导出，共包含 14 个现有测评会话。
- 375 px 宽度下测评、报告、教师中心和用户管理页面无横向溢出。
- 服务重启后登录令牌与页面访问可以恢复。

## 验收中修复

1. 转录校订页面存在嵌套 `<main>`，已改为带名称的普通分区，主内容语义恢复唯一。
2. 学生能够看到未发布报告草稿，已改为学生列表和详情只返回 `published` 报告。
3. 报告生成阶段不再提前通知学生；教师正式发布后才产生“报告已发布”通知。
4. 新增报告可见性自动化测试，覆盖学生草稿拒绝、学生已发布允许和教师草稿允许。

## 正式采集前阻塞项

### 1. 真实麦克风闭环未完成

验收浏览器未获得麦克风权限，页面正确进入“录音尚未启动，任务暂不能提交”状态。需要在实际 Chrome/Edge 中完成一次：授权麦克风、朗读 30 秒以上、提交任务、试听导出 WAV、核对 ASR 文本。

### 2. 当前 LLM 配置不可用

运行时配置为 `https://localhost:11434/v1`，诊断返回 SSL 协议错误。本地 Ollama 通常应使用 `http://localhost:11434/v1`；云端采集应改为实际火山方舟 HTTPS 地址和接入点 ID。

### 3. 当前 ASR 公网链路不可用

豆包语音诊断返回 `45000006 Invalid audio URI`，公网地址 `https://www.21050411.xyz` 探针返回 HTTP 403。正式采集前必须确认该域名或 Tunnel 能公开访问签名音频地址，并在云端重新运行语音探针。

### 4. 现有演示数据不满足研究质量门槛

当前 7 次完整测评中，可纳入样本为 0；主要缺少可播放标准音频与非空权威转录。现有演示数据不能作为正式采集链路通过的证据。

## 自动化验证

- 前端 TypeScript 与生产构建：通过。
- 后端自动化测试：102 项全部通过。
- 前端主包：约 189 KB，无 500 KB 主包警告。
