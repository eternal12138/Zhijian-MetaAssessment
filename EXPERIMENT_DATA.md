# 实验数据收集与导出

系统保存经知情同意产生的任务过程数据、录音、转录、AI 候选、人工复核结果和附录二任务型元认知问卷。导出为实名受控研究操作，不进行匿名化，只有管理员和职责范围内的教师可以使用。

## 收集内容

- 浏览器录音分片及时间、大小和格式元数据；
- 服务端 ASR、浏览器实时字幕及人工转录/校订版本；
- 最新 AI 抽取版本的候选文本，以及与其并存的人工复核状态；
- 人工补充的遗漏候选，单独标记为“人工补充”，不覆盖 AI 候选；
- 测评任务、顺序、协议版本、参与者身份和研究质量状态；
- 24 道问卷的 1–7 原始作答、反向计分、总分和分维度结果；
- 双人盲编、共识与仲裁结果，通过独立的专家训练数据入口导出。

## 导出入口

### 问卷答题矩阵

生成 UTF-8 BOM CSV。每行是一名参与者的一次完整测评，依次包含系统姓名、问卷填写姓名、账号、班级、用户/测评 ID、质量状态、24 道题原始作答、计分结果、分维度结果和报告状态。教师只获得其管理班级的数据，管理员获得全部授权数据。

### 转录、候选与可选录音数据包

生成中文目录 ZIP。创建时先选择范围：

- `全部已完成测评`：生成当前完整快照；
- `仅导出上次之后新增内容`：包含相对相同导出条件的新完成测评，以及最新抽取版本中新复核的候选；
- `仅导出当前已接受内容`：只生成用户索引和当前最新抽取版本中人工接受的候选，不包含录音、原始转录、待复核或已排除候选。

在“全部”或“仅新增”下还可以决定是否包含录音：

- 勾选录音：后台加载或生成标准 WAV，并写入录音清单、声音检测值和 SHA-256；
- 不勾选录音：跳过音频加载、合并、转码、校验和打包，仅生成文本材料，速度更快、文件更小。

含录音导出具有独立保护水位：一次不含录音的导出不会被误认为已经归档过历史录音；之后选择“增量＋录音”时，以最近一次真正包含录音的导出为基准。不含录音的增量可以复用最近一次完整文本快照（无论该快照是否附带录音）。若不存在可用的同类历史导出，增量导出自动按首次完整范围生成。

## 当前 ZIP 结构

完整且勾选录音时：

```text
00_用户信息/
  用户信息.csv
01_原始录音/
  录音文件清单.csv
  <账号_用户名_问卷填写姓名>/
    <任务序号_任务名称_会话编号>.wav
02_原始转录文本/
  原始转录文本.csv
03_AI筛选后的转录文本/
  AI筛选后的转录文本.csv
04_AI筛选并人工校对的文本/
  AI筛选并人工校对的文本.csv
导出数据说明.txt
导出清单.json
```

不勾选录音时不会创建 `01_原始录音`。选择“仅导出当前已接受内容”时只创建 `00_用户信息`、`04_AI筛选并人工校对的文本`、说明和清单。所有文本 CSV 的最前方均为账号、用户名、问卷填写姓名和班级。

`02` 中 ASR 与人工转录写在同一个“原转录文本”列，并由“原转录来源”标记。`03` 包含最新实际生成候选的抽取版本中的 AI 候选及复核状态。`04` 只包含人工接受的文本，人工补充遗漏会标记来源类型。

## 增量口径与人工复核警告

- 页面中的“累计已接受”是当前最新抽取版本的全部接受数；
- “新增接受”是相对所选导出条件的上次快照之后新接受的数量，两者不应被理解为相等；
- 增量判断使用服务器保存的 UTC 快照水位；
- 旧抽取版本后来发生的修改不会冒充最新版本新增数据；
- 尚有待复核候选或已完成任务尚无候选时，系统会警告并要求明确确认；该导出属于过程版本，不能作为最终人工复核数据；
- 历史导出的“复核完成”状态同时考虑待复核数量和无候选任务，不只检查待复核数。

## 后台生成、下载与空间管理

录音转录 ZIP 由独立 `export-worker` 领取数据库队列任务，网页轮询只读取进度，不会重复启动生成进程。标准 WAV 路径和音频元数据会持久化，后续导出不重复转码同一录音。压缩包先写入 `.part` 临时文件，成功后原子发布；失败时删除临时文件。

下载使用短期签名地址并支持流式传输。下载完成后服务器只保留最近一次派生 ZIP，历史导出任务保留审计状态但清除旧文件路径。原始录音不属于派生导出文件，不会被此清理流程删除。

## 专家训练数据

管理员在“研究管理 → 专家双人编码”中单独导出训练 CSV，可选择 `clean_text` 或 `raw_text`，以及共识/仲裁标签或每位专家独立标签。只有存在合法专家标签的数据会进入训练数据导出；该入口不包含音频。

## 安全注意事项

- 创建导出、生成下载凭证和下载行为均写入审计日志；
- 数据包含姓名、账号、班级和数据库 ID，仅限获授权研究人员使用；
- 下载后应存放在受控目录，不应上传到公开网盘或代码仓库；
- 如需对外共享，应另行生成脱敏副本；
- 正式实验前应完成一次端到端验收，核对身份字段、录音可播放、转录来源、候选版本和人工复核状态。
